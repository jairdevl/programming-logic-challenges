# Program simulate launch currency
import random

# Function to simulate the launch
def simulate_launch():
    return random.choice(["Face", "Cross"])

input("Press Enter to simulate the launch...")
print(f"The result of the launch is: {simulate_launch()}")

